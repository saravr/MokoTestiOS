# Uncomment the next line to define a global platform for your project
platform :ios, '14.1'

target 'MokoTestiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MokoTestiOS
  pod 'shared', :path => '../MokoTest/shared/build/cocoapods/publish/debug/shared.podspec'

  target 'MokoTestiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MokoTestiOSUITests' do
    # Pods for testing
  end

end
